/* this is a block comment
 * it can go on multiple lines
 */

var str: string = "\u0009string\n\tmulti line";
var something: bool;

fn get(ints: int, idx: int): int {
	return ints+idx;
}

fn main() {
	const αConst: int = 69;
	var a: int = 0x1A4;

	a = αConst * 3 + 1.0;

	// line comment
	if a == 50 {
		println("value of a:", 50);
	} elif a != 30 {
		a += 3;
		println(string);
	} else {
		print('\x41' /* dikke neuke */ );
	}

	var ints: int = 1 + // nikke deuke
	1;
	println("ints[a] =", get(ints, a), "| a =", a);
}
