/* this is a block comment
 * it can go on multiple lines
 */

var str: string = "\u0009string\n\tmulti line";
var something: bool;

// fn get(ints: int, idx: int): int {
// 	return ints+idx;
// }

fn main() {
	const αConst: int = 69;
	var a: int = 0x1A4;
	var b: bool = true;
	var c: string = "3";

	a = αConst * 3 + 1;

	// line comment

	if b {
		something = true;
	}

	// // if a == 50 {
		// b = false;
		// println("value of a:", a);
	// }

	if a == 208 {
		something = false;
	}
	// elif a != 30 {
	// 	a += 3;
	// 	// println(str);
	// } else {
	// 	// print('\x41' /* dikke neuke */ );
	// }

	var ints: int = 1 + // nikke deuke
	1;
	// println("ints[a] =", get(ints, a), "| a =", a);
}

